# API Configuration
# Port number for the API server
PORT=4000
# Environment mode: development, production, or test
NODE_ENV=development

# Database
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/rapidphoto

# Authentication
# Secret key for Better-Auth session encryption
# Generate a secure random string (e.g., using: openssl rand -base64 32)
BETTER_AUTH_SECRET=your-secret-key-here
# Base URL for Better-Auth callbacks and session management
# In production, use your Railway API URL: https://api-production-xxxx.railway.app
BETTER_AUTH_URL=http://localhost:4000

# Cloudflare R2 Configuration
# Cloudflare account ID (found in Cloudflare dashboard)
R2_ACCOUNT_ID=your_account_id_here
# R2 API access key (create in Cloudflare dashboard > R2 > Manage R2 API Tokens)
R2_ACCESS_KEY_ID=your_access_key_id_here
# R2 API secret key (create in Cloudflare dashboard > R2 > Manage R2 API Tokens)
R2_SECRET_ACCESS_KEY=your_secret_access_key_here
# R2 bucket name for photo storage
R2_BUCKET_NAME=rapidphoto-uploads
# Public URL for R2 bucket (optional, not used with presigned URLs)
# Format: https://your-account-id.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://your-account-id.r2.cloudflarestorage.com

# Redis Configuration (for job queues)
# Redis connection string for BullMQ job queues
# Format: redis://user:password@host:port
# Optional for MVP, required for background jobs (AI tagging, thumbnails)
REDIS_URL=redis://localhost:6379

# AWS Rekognition Configuration (for AI tagging)
# AWS access key ID for Rekognition service
# Create in AWS IAM > Users > Security credentials > Access keys
AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS secret access key for Rekognition service
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS region for Rekognition service (e.g., us-east-1, us-west-2)
AWS_REGION=us-east-1

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Format: http://localhost:3000,https://your-web-domain.com
# Default: http://localhost:3000 (automatically included)
ALLOWED_ORIGINS=http://localhost:3000