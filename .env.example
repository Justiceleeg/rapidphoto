# Root Environment Variables
# This file contains general configuration for the monorepo
# For app-specific variables, see:
# - apps/api/.env.example (Backend API)
# - apps/web/.env.example (Web frontend - if exists)
# - apps/mobile/.env.example (Mobile app - if exists)

# API Configuration
# Port number for the API server (used by apps/api)
API_PORT=4000
# Base URL for the API server (used by frontend apps)
API_URL=http://localhost:4000

# Database
# PostgreSQL connection string (used by apps/api)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/rapidphoto

# Authentication
# Secret key for authentication (used by apps/api)
# Generate a secure random string (e.g., using: openssl rand -base64 32)
AUTH_SECRET=your-secret-key-here
# Base URL for authentication callbacks (used by apps/api)
AUTH_URL=http://localhost:4000

# Environment
# Environment mode: development, production, or test
NODE_ENV=development
