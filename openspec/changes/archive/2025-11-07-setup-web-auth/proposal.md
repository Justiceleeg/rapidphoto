# Change: Setup Web Frontend - Auth

## Why
Initialize the Next.js 15 web application, configure it for the monorepo, setup React Query and state management, implement authentication client integration, and create login/register pages with authentication flow. This enables users to register and login through the web interface.

## What Changes
- Initialize Next.js 15 project in `apps/web`
- Configure for monorepo with workspace dependencies
- Setup React Query providers and state management
- Implement authentication client and hooks
- Create login and register pages
- Implement dashboard layout with authentication guard

## Impact
- Affected specs: web (new capability)
- Affected code: `apps/web/` directory structure, authentication pages, and API client package

