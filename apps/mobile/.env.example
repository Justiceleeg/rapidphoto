# Mobile App Environment Variables
# Expo automatically loads environment variables prefixed with EXPO_PUBLIC_
# Alternatively, configure in app.json under expo.extra

# API Configuration
# Backend API URL for API requests
# In development: http://localhost:4000
# In production: https://api-production-xxxx.railway.app (your Railway API URL)
# Note: For local development, you may need to use your computer's IP address instead of localhost
EXPO_PUBLIC_API_URL=http://localhost:4000

# Alternative: Configure in app.json
# {
#   "expo": {
#     "extra": {
#       "apiUrl": "http://localhost:4000"
#     }
#   }
# }

